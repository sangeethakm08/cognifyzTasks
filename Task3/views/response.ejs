<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Submission Successful</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary-gradient: linear-gradient(135deg, #a8e6cf 0%, #dcedc8 100%);
      --success-gradient: linear-gradient(135deg, #7fcdbb 0%, #a8e6cf 100%);
      --glass-bg: rgba(255, 255, 255, 0.85);
      --glass-border: rgba(45, 80, 22, 0.2);
      --shadow-soft: 0 8px 32px 0 rgba(127, 205, 187, 0.3);
      --shadow-hover: 0 12px 40px 0 rgba(127, 205, 187, 0.4);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: var(--primary-gradient);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      position: relative;
      overflow-x: hidden;
    }

    /* Static Background */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 20% 50%, rgba(168, 230, 207, 0.3) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(127, 205, 187, 0.3) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(255, 211, 182, 0.2) 0%, transparent 50%);
      z-index: 0;
    }

    body::after {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.03) 50%, transparent 70%),
        linear-gradient(-45deg, transparent 30%, rgba(255, 255, 255, 0.03) 50%, transparent 70%);
      background-size: 100px 100px;
      z-index: 0;
      opacity: 0.4;
    }

    .success-container {
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.75) 100%);
      backdrop-filter: blur(25px);
      -webkit-backdrop-filter: blur(25px);
      border: 2px solid var(--glass-border);
      border-radius: 28px;
      padding: 50px;
      max-width: 700px;
      width: 100%;
      box-shadow: var(--shadow-soft);
      text-align: center;
      animation: scaleIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      z-index: 1;
    }

    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.9) translateY(20px);
      }
      to {
        opacity: 1;
        transform: scale(1) translateY(0);
      }
    }

    .success-icon {
      width: 120px;
      height: 120px;
      background: var(--success-gradient);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px;
      animation: bounceIn 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55);
      box-shadow: 0 10px 30px rgba(127, 205, 187, 0.4);
      position: relative;
    }

    .success-icon::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: 50%;
      background: var(--success-gradient);
      opacity: 0.3;
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes bounceIn {
      0% {
        transform: scale(0);
      }
      50% {
        transform: scale(1.15);
      }
      100% {
        transform: scale(1);
      }
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
        opacity: 0.3;
      }
      50% {
        transform: scale(1.2);
        opacity: 0;
      }
    }

    .success-icon i {
      font-size: 60px;
      color: white;
      position: relative;
      z-index: 1;
    }

    .success-title {
      font-size: 2.5rem;
      font-weight: 700;
      background: linear-gradient(135deg, #2d5016 0%, #4a7c2a 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 15px;
      letter-spacing: -0.5px;
    }

    .success-message {
      color: #2d5016;
      font-size: 1.1rem;
      margin-bottom: 40px;
      font-weight: 500;
    }

    .data-display {
      background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.8) 100%);
      backdrop-filter: blur(15px);
      border: 2px solid rgba(45, 80, 22, 0.15);
      border-radius: 20px;
      padding: 35px;
      margin-bottom: 35px;
      text-align: left;
    }

    .data-item {
      padding: 18px 0;
      border-bottom: 1px solid rgba(45, 80, 22, 0.1);
      display: flex;
      align-items: flex-start;
      gap: 15px;
    }

    .data-item:last-child {
      border-bottom: none;
    }

    .data-icon {
      font-size: 1.3rem;
      color: #4a7c2a;
      margin-top: 2px;
      min-width: 24px;
    }

    .data-content {
      flex: 1;
    }

    .data-label {
      font-weight: 600;
      color: #2d5016;
      display: block;
      margin-bottom: 6px;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .data-value {
      color: #2d5016;
      font-size: 1.05rem;
      font-weight: 500;
    }

    .interests-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 8px;
    }

    .interest-tag {
      background: rgba(127, 205, 187, 0.3);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(45, 80, 22, 0.2);
      color: #2d5016;
      padding: 8px 18px;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .interest-tag:hover {
      background: rgba(127, 205, 187, 0.4);
      transform: translateY(-2px);
    }

    .back-button {
      background: linear-gradient(135deg, #7fcdbb 0%, #a8e6cf 100%);
      backdrop-filter: blur(10px);
      border: 2px solid rgba(45, 80, 22, 0.2);
      border-radius: 14px;
      padding: 14px 40px;
      font-size: 1rem;
      font-weight: 600;
      color: #ffffff;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 15px rgba(127, 205, 187, 0.3);
    }

    .back-button:hover {
      background: linear-gradient(135deg, #6bc4a6 0%, #a8e6cf 100%);
      border-color: rgba(45, 80, 22, 0.3);
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(127, 205, 187, 0.4);
      color: #ffffff;
    }

    .back-button i {
      font-size: 1.1rem;
    }

    @media (max-width: 768px) {
      .success-container {
        padding: 40px 30px;
      }

      .success-title {
        font-size: 2rem;
      }

      .data-display {
        padding: 25px;
      }
    }

    @media (max-width: 576px) {
      .success-container {
        padding: 35px 25px;
        border-radius: 20px;
      }

      .success-title {
        font-size: 1.75rem;
      }

      .success-icon {
        width: 100px;
        height: 100px;
      }

      .success-icon i {
        font-size: 50px;
      }

      .data-item {
        flex-direction: column;
        gap: 8px;
      }

      .data-label {
        margin-bottom: 4px;
      }
    }
  </style>
</head>
<body>
  <div class="success-container">
    <div class="success-icon">
      <i class="bi bi-check-circle-fill"></i>
    </div>
    
    <h1 class="success-title">Submission Successful!</h1>
    <p class="success-message">Thank you for contacting us. We have received your information and will get back to you soon.</p>

    <div class="data-display">
      <div class="data-item">
        <i class="bi bi-person data-icon"></i>
        <div class="data-content">
          <span class="data-label">Full Name</span>
          <span class="data-value"><%= name %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-envelope data-icon"></i>
        <div class="data-content">
          <span class="data-label">Email Address</span>
          <span class="data-value"><%= email %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-telephone data-icon"></i>
        <div class="data-content">
          <span class="data-label">Phone Number</span>
          <span class="data-value"><%= phone %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-gender-ambiguous data-icon"></i>
        <div class="data-content">
          <span class="data-label">Gender</span>
          <span class="data-value"><%= typeof gender !== 'undefined' && gender ? gender : 'N/A' %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-calendar-event data-icon"></i>
        <div class="data-content">
          <span class="data-label">Date of Birth</span>
          <span class="data-value"><%= typeof dob !== 'undefined' && dob ? dob : 'N/A' %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-lock data-icon"></i>
        <div class="data-content">
          <span class="data-label">Password</span>
          <span class="data-value">••••••••</span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-globe data-icon"></i>
        <div class="data-content">
          <span class="data-label">Country</span>
          <span class="data-value"><%= country %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-briefcase data-icon"></i>
        <div class="data-content">
          <span class="data-label">Occupation</span>
          <span class="data-value"><%= typeof occupation !== 'undefined' && occupation ? occupation : 'N/A' %></span>
        </div>
      </div>

      <div class="data-item">
        <i class="bi bi-chat-left-text data-icon"></i>
        <div class="data-content">
          <span class="data-label">Message</span>
          <span class="data-value"><%= message %></span>
        </div>
      </div>
    </div>

    <a href="/" class="back-button">
      <i class="bi bi-arrow-left"></i>
      Submit Another Form
    </a>
  </div>
</body>
</html>
